% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{plan_warp_reads}
\alias{plan_warp_reads}
\title{Plan efficient source reads for a warp operation}
\usage{
plan_warp_reads(
  src_crs,
  src_gt,
  src_dim,
  dst_crs,
  dst_gt,
  dst_off,
  dst_size,
  resample_padding = 0L,
  min_fill_ratio = 0.5,
  min_dst_size = 8L
)
}
\arguments{
\item{src_crs}{Character CRS string for source}

\item{src_gt}{Numeric length-6 source geotransform}

\item{src_dim}{Integer c(ncol, nrow) of source raster}

\item{dst_crs}{Character CRS string for destination}

\item{dst_gt}{Numeric length-6 destination geotransform}

\item{dst_off}{Integer c(col_off, row_off) destination window offset}

\item{dst_size}{Integer c(ncol, nrow) destination window size}

\item{resample_padding}{Integer kernel padding}

\item{min_fill_ratio}{Numeric threshold for subdivision (default 0.5)}

\item{min_dst_size}{Integer minimum chunk dimension (default 8)}
}
\value{
A list of chunk plans
}
\description{
Given a destination tile and source/destination CRS and geotransform,
compute the optimal set of source reads. For tiles that straddle the
antimeridian, this produces a split-read plan that avoids reading the
entire source latitude band.
}
