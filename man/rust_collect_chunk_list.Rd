% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rust_collect_chunk_list}
\alias{rust_collect_chunk_list}
\title{Recursively subdivide a destination window into chunks with efficient source windows.}
\usage{
rust_collect_chunk_list(
  src_crs,
  src_gt,
  src_dim,
  dst_crs,
  dst_gt,
  dst_off,
  dst_size,
  resample_padding,
  min_fill_ratio,
  min_dst_size
)
}
\arguments{
\item{src_crs}{Character CRS string for source}

\item{src_gt}{Numeric vector length 6 (source geotransform)}

\item{src_dim}{Integer vector c(ncol, nrow) of source raster}

\item{dst_crs}{Character CRS string for destination}

\item{dst_gt}{Numeric vector length 6 (destination geotransform)}

\item{dst_off}{Integer vector c(col_off, row_off) destination window offset}

\item{dst_size}{Integer vector c(ncol, nrow) destination window size}

\item{resample_padding}{Integer kernel padding (0=near, 1=bilinear, 2=cubic, 3=lanczos)}

\item{min_fill_ratio}{Numeric threshold for subdivision (default 0.5)}

\item{min_dst_size}{Integer minimum destination dimension to allow splitting (default 8)}
}
\value{
List of chunk plan lists
}
\description{
When a destination tile straddles a coordinate singularity (e.g. the antimeridian),
the source window can span the entire source raster. This function recursively
splits the destination until each chunk has a compact source window.
}
\details{
Returns a list of chunk plans, each containing:
\itemize{
\item src_xoff, src_yoff, src_xsize, src_ysize: source window
\item dst_xoff, dst_yoff, dst_xsize, dst_ysize: destination sub-window
\item fill_ratio: source fill ratio for this chunk
}
}
