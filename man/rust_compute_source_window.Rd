% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rust_compute_source_window}
\alias{rust_compute_source_window}
\title{Compute the source pixel window needed for a destination window.}
\usage{
rust_compute_source_window(
  src_crs,
  src_gt,
  src_dim,
  dst_crs,
  dst_gt,
  dst_off,
  dst_size,
  resample_padding
)
}
\arguments{
\item{src_crs}{Character CRS string}

\item{src_gt}{Numeric vector length 6 (source geotransform)}

\item{src_dim}{Integer vector c(ncol, nrow) — full source raster size}

\item{dst_crs}{Character CRS string}

\item{dst_gt}{Numeric vector length 6 (dest geotransform)}

\item{dst_off}{Integer vector c(col_off, row_off) — dest window offset}

\item{dst_size}{Integer vector c(ncol, nrow) — dest window size}

\item{resample_padding}{Integer — extra pixels for resampling kernel
(0 = nearest, 1 = bilinear, 2 = cubic, 3 = lanczos)}
}
\value{
List with xoff, yoff, xsize, ysize, fill_ratio, n_failed, n_samples
or NULL if computation fails (too few valid transform points)
}
\description{
This is the GDAL ComputeSourceWindow equivalent: given a destination
pixel window (offset + size) and CRS/geotransform specs for both source
and destination, determine which source pixels are needed.
}
\details{
Uses the same algorithm as GDAL: edge sampling with grid fallback,
antimeridian heuristic, resampling padding.
}
